{
  "PGRST_DB_URI": {
    "value": "postgres://postgres:${{postgres.POSTGRES_PASSWORD}}@${{postgres.RAILWAY_PRIVATE_DOMAIN}}:5432/postgres",
    "description": "PostgreSQL connection string for PostgREST. Uses private networking."
  },
  "PGRST_DB_SCHEMAS": {
    "value": "public,storage,graphql_public",
    "description": "Comma-separated list of schemas to expose via REST API."
  },
  "PGRST_DB_ANON_ROLE": {
    "value": "anon",
    "description": "Database role for anonymous (unauthenticated) requests."
  },
  "PGRST_JWT_SECRET": {
    "value": "${{auth.JWT_SECRET}}",
    "description": "HS256 secret for verifying JWTs. References the auth service JWT_SECRET."
  }
}
